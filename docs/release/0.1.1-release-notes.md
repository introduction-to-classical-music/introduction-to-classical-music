# 不全书 0.1.1 发布说明

`0.1.1` 是在公开 `dev` 完成人工复测问题修复后的补丁版本。

本版本修复了活动 library 同步、连续冲突合并、`.icmlibrary` 导入、库目录改名和本地网站导出样式问题，并将目录详情导出改为可折叠 HTML 条目树。

安装版默认仍使用空资料库，用户资料保存于 `%APPDATA%\buquanshu`。官方 library、软件开发目录和站点导出目录继续保持独立。
